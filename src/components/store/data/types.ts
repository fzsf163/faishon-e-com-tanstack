import { JSX } from 'react'

export interface StoreTopNavBarTypes {
  title: string // Title of the section
  text: string // Descriptive text or subtitle
  icon: JSX.Element // Icon component (from a library like react-icons)
}
